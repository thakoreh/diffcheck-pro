/** @type {import('next').MetadataConfig} */
const siteConfig = {
  name: 'DiffCheck Pro',
  description: 'Advanced text & code comparison tool. Privacy-first, client-side diff with syntax highlighting, word-level diff, and export. Free to use.',
  url: 'https://diffcheck-pro.vercel.app',
  ogImage: '/og.png',
}

module.exports = siteConfig
